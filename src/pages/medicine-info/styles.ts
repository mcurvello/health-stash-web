import styled from "styled-components";

const Container = styled.div`
  width: 1040px;
`

const ContainerSimilarMedicines = styled.div`
  margin: 20px 0;
  display: flex;
  gap: 20px;
`

const ContainerButtons = styled.div`
  display: flex;
  justify-content: space-between;
  width: 100%;
`

export { Container, ContainerButtons, ContainerSimilarMedicines }